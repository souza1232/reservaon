// Stub para testes: o pacote real "server-only" lança erro fora do bundler
// do Next.js. Em Vitest (Node puro) isso não se aplica, então usamos um
// módulo vazio no lugar via alias em vitest.config.ts.
export {};
